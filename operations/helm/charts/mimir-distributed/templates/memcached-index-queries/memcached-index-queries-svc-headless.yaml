{{- if .Values.memcached_index_queries.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimir.resourceName" (dict "ctx" . "component" "memcached-index-queries") }}-headless
  labels:
    {{- include "mimir.labels" (dict "ctx" . "component" "memcached-index-queries") | nindent 4 }}
    prometheus.io/service-monitor: "false"
    {{- with .Values.memcached_index_queries.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- toYaml .Values.memcached_index_queries.service.annotations | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: memcached-client
      port: {{ .Values.memcached_index_queries.port }}
      targetPort: {{ .Values.memcached_index_queries.port }}
    {{ if .Values.memcached_index_queries.metricsEnabled }}
    - name: exporter-http-metrics
      port: 9150
      targetPort: 9150
    {{ end }}
  selector:
    {{- include "mimir.selectorLabels" (dict "ctx" . "component" "memcached-index-queries") | nindent 4 }}
{{- end -}}
