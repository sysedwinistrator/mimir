{{- if .Values.memcached_metadata.enabled -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "mimir.resourceName" (dict "ctx" . "component" "memcached-metadata") }}
  labels:
    {{- include "mimir.labels" (dict "ctx" . "component" "memcached-metadata") | nindent 4 }}
  annotations:
    {{- toYaml .Values.memcached_metadata.annotations | nindent 4 }}
spec:
  podManagementPolicy: {{ .Values.memcached_metadata.podManagementPolicy }}
  replicas: {{ .Values.memcached.replicas }}
  selector:
    matchLabels:
      {{- include "mimir.selectorLabels" (dict "ctx" . "component" "memcached-metadata") | nindent 6 }}
  updateStrategy:
    {{- toYaml .Values.memcached_metadata.statefulStrategy | nindent 4 }}
  serviceName: {{ template "mimir.fullname" . }}-memcached-metadata

  template:
    metadata:
      labels:
        {{- include "mimir.podLabels" (dict "ctx" . "component" "memcached-metadata") | nindent 8 }}
        {{- with .Values.memcached_metadata.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with .Values.memcached_metadata.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}

    spec:
      serviceAccountName: {{ template "mimir.serviceAccountName" . }}
      {{- if .Values.memcached_metadata.priorityClassName }}
      priorityClassName: {{ .Values.memcached_metadata.priorityClassName }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.memcached_metadata.securityContext | nindent 8 }}
      initContainers:
        {{- toYaml .Values.memcached_metadata.initContainers | nindent 8 }}
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
      {{- range .Values.image.pullSecrets }}
        - name: {{ . }}
      {{- end }}
      {{- end }}
      nodeSelector:
        {{- toYaml .Values.memcached_metadata.nodeSelector | nindent 8 }}
      affinity:
        {{- toYaml .Values.memcached_metadata.affinity | nindent 8 }}
      tolerations:
        {{- toYaml .Values.memcached_metadata.tolerations | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.memcached_metadata.terminationGracePeriodSeconds }}
      containers:
        {{- if .Values.memcached.extraContainers }}
        {{ toYaml .Values.memcached_metadata.extraContainers | nindent 8 }}
        {{- end }}
        - name: memcached
          image: {{ .Values.memcached_metadata.image.repository}}:{{ .Values.memcached_metadata.image.tag}}
          imagePullPolicy: {{ .Values.memcached_metadata.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.memcached_metadata.port }}
              name: client
          args:
            - -m {{ .Values.memcached_metadata.allocatedMemory }}
            - -o
            - modern
            - -I 1m
            - -c 16384
            - -v
            - -u {{ .Values.memcached_metadata.port }}
            {{- range $key, $value := .Values.memcached_metadata.extraArgs }}
            - "-{{ $key }} {{ $value }}"
            {{- end }}

      {{- if .Values.memcached_metadata.metricsEnabled }}
        - name: exporter
          image: prom/memcached-exporter:v0.6.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9150
              name: http-metrics
          args:
            - "--memcached.address=localhost:{{ .Values.memcached_metadata.port }}"
            - "--web.listen-address=0.0.0.0:9150"
      {{- end }}
{{- end -}}
